<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cognito">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.example.cognito.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.cognito.OptionsMenuActivity"></activity>
        <activity
            android:name="com.example.cognito.PoetryServiceActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name="com.example.cognito.login.LogoutActivity" />
        <activity android:name="com.example.cognito.login.SignInActivity" />
        <activity android:name="com.example.cognito.login.ValidateActivity" />
        <activity android:name="com.example.cognito.login.SignUpActivity" />
        <activity android:name="com.example.cognito.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>